/**
 * Operadores Lógicos
 */

//And, OR y Not - Y, O y Negación(NO)


// && : Operador Y
// || : OPERADOR O
// !  : OPERADOR DE NEGACIÓN

const a = 10
const b = 20
const c = "10"

(a == b && a ===c) //false
(a != b || a === c) //true
!(a == c) // true